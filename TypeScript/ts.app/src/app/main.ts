import {addProduct} from './products/product.service';

addProduct({
  id: '1',
  title: 'p1',
  creatAt: new Date(),
  stock: 90,

  category: {
    id: string | number;
    name: string;
  }
  }
})
